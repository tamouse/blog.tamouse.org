In [[http://stackoverflow.com/questions/135688/setting-environment-variables-in-os-x]], the general consensus is to change the @@/etc/launch.conf@@ file. The simplest way is to use the @@launchctl@@ command:

(:source lang=bash:)
sudo launchctl setenv PATH /usr/local/bin:/usr/local/sbin:/opt/local/bin:/opt/local/sbin:/bin:/sbin:/usr/bin:/usr/sbin
(:sourceend:)

One can also set the path by putting the command in the @@/etc/launch.conf@@ file:

(:source:)
setenv PATH /usr/local/bin:/usr/local/sbin:/opt/local/bin:/opt/local/sbin:/bin:/sbin:/usr/bin:/usr/sbin
(:sourceend:)


(:Summary:a quick `HowTo to setting environment variables in OSX:)
(:Parent:(Technology.)OSX:)
(:IncludeMe:[[(Technology.)OSX]]:)
(:Categories:[[!HowTos]]:)
(:Tags: mac, osx, sysadmin:)
(:tags-hide mac, osx, sysadmin :)



